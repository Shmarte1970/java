/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Condicionales;

import java.io.IOException;

/**
 *
 * @author USER
 */
public class Condicionales {
    
    
    // Aprovado
    
    
    public static void main(String[] args) throws IOException {
    
        // Asigna el valor que desees a la variable 'i'
        /*
         int nota = 7; // La nota del examen, puedes asignarle cualquier valor entre 0 y 10

        if (nota > 5) {
            System.out.println("Aprobado");
        } else if (nota == 5) {
            System.out.println("Aprobado por los pelos");
        } else {
            System.out.println("Suspenso");
        }
        
      */
    
        
        //  Positivo negativo
        
        /*
        
        int i = -15; 

        // Verificar si es positivo o negativo
        if (i >= 0) {
            System.out.println("El valor de 'i' es positivo");
        } else {
            System.out.println("El valor de 'i' es negativo");
        }

        */
        // Verificar si es par o impar
        
        /*
        if (i % 2 == 0) {
            System.out.println("El valor de 'i' es par");
        } else {
            System.out.println("El valor de 'i' es impar");
        }

        // Verificar si es múltiplo de 5
        if (i % 5 == 0) {
            System.out.println("El valor de 'i' es múltiplo de 5");
        } else {
            System.out.println("El valor de 'i' no es múltiplo de 5");
        }

        // Verificar si es múltiplo de 10
        if (i % 10 == 0) {
            System.out.println("El valor de 'i' es múltiplo de 10");
        } else {
            System.out.println("El valor de 'i' no es múltiplo de 10");
        }

        // Verificar si es mayor o menor que 100
        if (i > 100) {
            System.out.println("El valor de 'i' es mayor que 100");
        } else if (i < 100) {
            System.out.println("El valor de 'i' es menor que 100");
        } else {
            System.out.println("El valor de 'i' es igual a 100");
        }
*/
        
        // Positivo 
        
        /*
        
         int b = -10; // Asigna el valor que desees a la variable 'b'

        String mensaje = (b >= 0) ? "El valor de 'b' es positivo" : "El valor de 'b' es negativo";

        System.out.println(mensaje);
        */
        
          int i = 7; // Asigna el valor que desees a la variable 'i'

        String mensaje = (i % 2 == 0) ? "El valor de 'i' es par" : "El valor de 'i' es impar";

        System.out.println(mensaje);
    }
    
}
